spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/mohdadnansiddiqui/config-server.git

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://host.docker.internal:8761/eureka/